export interface IAudioRefs {
    current: HTMLAudioElement;
}

export interface IPlaysRefs {
    current: HTMLImageElement;
}
export interface IRangesRefs {
    current: HTMLInputElement;
}
export interface IRangesRefs {
    current: HTMLInputElement;
}

export interface ICurrentTimeRefs {
    current: HTMLSpanElement;
}

export interface IDurationRefs {
    current: HTMLSpanElement;
}

export interface IImageRefs {
    current: HTMLImageElement;
}

export interface IAboutRefs {
    current: HTMLDivElement;
}

export interface IMainBackgroundRef {
    current: HTMLDivElement;
}